kubectl apply -f namespace.yml

kubectl apply -f configmap.yml

kubectl apply -f eureka-server.yml

kubectl apply -f book-service.yml

kubectl apply -f review-service.yml

kubectl apply -f api-gateway.yml

kubectl apply -f unleash-postgres.yml

kubectl apply -f unleash.yml

# verify
kubectl get pods -n microservices
kubectl get svc -n microservices

# Persisted Volume
kubectl apply -f book-pv.yml
# Persisted Volume Claim
kubectl apply -f book-pvc.yml

# Get external IP:
kubectl get svc -n microservices api-gateway

# Recent cluster-wide events:
kubectl get events -A --sort-by=.lastTimestamp | tail -n

# Restart Deployment
kubectl rollout restart deployment book-service -n microservices
kubectl rollout restart deployment api-gateway -n microservices

# Access Unleash UI
kubectl port-forward svc/unleash 4242:4242 -n unleash
# Open in browser:
http://localhost:4242
# Default credentials: admin/unleash4all
# Create Client API token for SDK access for environment "development"
# Create feature toggle "book-service-enabled"
# paste the token in book-service.yml configmap under UNLEASH_API_TOKEN
# paste the token in api-gateway.yml configmap under UNLEASH_API_TOKEN
# delete deployments and services of book-service and api-gateway
kubectl delete deployment api-gateway -n micriservices && kubectl delete deployment book-service -n micriservices && kubectl delete servic api-gateway -n micriservices && kubectl delete service book-service -n micriservices && kubectl apply -f api-gateway.yml && kubectl apply -f book-service.yml

# check unleash logs
kubectl logs deployment/unleash -n unleash

